# Project Instructions

At the start of a new chat in this project, check `MODEL_SELECTION.md` before doing substantial work.

If the user's request is project work, briefly recommend the best model and intelligence setting first so they can restart with the right settings if needed. Keep the recommendation short, then continue only if the current settings are appropriate or the user wants to proceed.

Default recommendation for most work here: `gpt-5.4` + `Medium`.
